import AppShell from '@/components/app-shell'
import JobHunterClient from '@/components/job-hunter/JobHunterClient'

export default function JobHunterPage() {
    return (
        <AppShell>
            <JobHunterClient />
        </AppShell>
    )
}